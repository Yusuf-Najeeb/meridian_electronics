# Local: copy to .env.local
# Vercel: Project → Settings → Environment Variables
#   Set BACKEND_URL for every environment you use: Production, Preview, and (optional) Development.
#   Example (Render): https://meridian-electronics.onrender.com  (no trailing slash, no /api path)
BACKEND_URL=http://127.0.0.1:8000

# Copy to .env.local for dev. On Vercel: Project → Settings → Environment Variables.
# Use your deployed API origin only (no path suffix): https://xxxx.execute-api.region.amazonaws.com
BACKEND_URL=http://127.0.0.1:8000
